version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install --frozen-lockfile
    build:
      commands:
        - npx nx build church-management-app
  artifacts:
    baseDirectory: apps/church-management-app/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
